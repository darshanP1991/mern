apiVersion: v1
kind: Service
metadata:
  name: mern-service
  namespace: mern-app
spec:
  selector:
    app: mern
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer

